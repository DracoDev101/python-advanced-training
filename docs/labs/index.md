# 实验说明

每个实验都要求具备三类输出：

1. **可运行命令**：例如 `python -m pytest`、`python manage.py check --deploy`、`celery -A config inspect active`。
2. **可观察证据**：日志、SQL、metrics、trace、profile、队列状态或错误堆栈。
3. **生产解释**：说明该实验对应的线上风险和修复边界。

后续每个 lesson 会配套独立 lab 目录或文档。
